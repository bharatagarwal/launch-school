<% if session[:message] %>
  <%= "<p class=message>#{session.delete(:message)}</p>" %>
<% end %>

<ul>
  <% @filenames.each do |name| %>
  <li>
  <a href="<%= "/" + name %>"><%= name %></a>&nbsp;
  <a href="<%= '/' + name + '/edit' %>">edit</a>
  </li>
  <% end %>
</ul>