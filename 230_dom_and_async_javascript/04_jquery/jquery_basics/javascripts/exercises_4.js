// changing multiple css properties
/*

var $content = $('#content');

$content.css({
  fontSize: '21px',
  color: 'coral',
});

*/
// jQuery.ajax(url, settingsObject);
// jQuery.ajax(settingsObject);

jQueryCollection[0] -> dom element
jQueryCollection.eq -> gives jquery element back
.each -> callback(index, el)
this inside each is the current dom element
$(this).attr() === this.getAttributeOf()

.eq(-1) -> last
.eq(-2) -> second from last

eq === equivalent?