a method that returns the sum of two integers

casual pseudocode

- Check the two inputs are integers
- If they are strings, check if the string conversion of their integer conversion equals the string itself i.e. that they are integer-only strings.
- Add the value of the two strings and return the added value.

formal pseudocode

START

# Given two inputs, num1 & num2.

IF the two inputs are not integers
THEN check whether they are string inputs of integers,
CONVERT string values to integer values

RETURN sum of the two integers

EBD

